# Project File Structure Explanation

## ✅ ESSENTIAL FILES (DO NOT DELETE)

### Core Application Code
```
rag_eval/
├── __init__.py          ← Package initialization, exports classes
├── cli.py               ← Command-line interface (main entry point)
├── evaluator.py         ← RAGEvaluator class with metrics
└── strategies.py        ← 4 chunking strategies + MockEmbedder
```
**Why needed:** This is your actual application code. Deleting these breaks the system.

---

### Configuration Files
```
requirements.txt         ← Python dependencies (llama-index, pypdf, etc.)
setup.py                 ← Package installation config (for pip install -e .)
queries.json             ← Your evaluation queries
```
**Why needed:**
- `requirements.txt` - Install dependencies with `pip install -r requirements.txt`
- `setup.py` - Makes `rag_eval` importable as a package
- `queries.json` - Your test queries; edit to add more

---

### Documentation
```
README.md                ← Project overview, quick start
GUIDE.md                 ← Complete user guide (setup, usage, customization)
PDF_GUIDE.md             ← PDF-specific instructions
RESULTS_ANALYSIS.md      ← Detailed analysis of your evaluation results
```
**Why needed:** User documentation. You or others can understand the project.

---

### Tests
```
tests/
└── test_basic.py        ← Unit tests for chunking strategies
```
**Why needed:** Validates code works correctly. Run with `pytest tests/test_basic.py`

---

### Sample Data
```
docs/
└── sample.md            ← Sample document for testing
```
**Why needed:** Example document for evaluation. Add your PDFs here.

---

### Output Files
```
evaluation_results.json  ← Generated results from last run
show_results.py          ← Script to display results nicely
```
**Why needed:**
- `evaluation_results.json` - Your evaluation data (can regenerate by running CLI)
- `show_results.py` - Convenient results viewer

---

### Version Control
```
.git/                    ← Git repository (commit history)
.gitignore               ← Files to exclude from git
```
**Why needed:** Track changes, collaborate, push to GitHub

---

### Python Virtual Environment
```
.venv/                   ← Isolated Python environment
```
**Why needed:** Keeps project dependencies separate from system Python

---

## ❌ REMOVED FILES (Auto-generated, not needed in git)

### Python Cache Files
```
__pycache__/             ← Compiled Python bytecode
*.pyc                    ← Individual bytecode files
```
**Why removed:** Auto-generated when you run Python. Regenerates automatically. Wastes git space.

### Build Artifacts
```
rag_eval.egg-info/       ← Package metadata
.pytest_cache/           ← Test cache
```
**Why removed:** Created by `pip install -e .` and `pytest`. Regenerates when needed.

### Duplicate Files
```
tests/sample.md          ← Duplicate of docs/sample.md
```
**Why removed:** Redundant. Use docs/sample.md instead.

---

## 📁 Final Clean Structure

```
Chunk-RAG-Python/
├── .git/                       # Git repository
├── .gitignore                  # Git ignore rules
├── .venv/                      # Virtual environment (local only)
│
├── rag_eval/                   # Main application package
│   ├── __init__.py
│   ├── cli.py
│   ├── evaluator.py
│   └── strategies.py
│
├── tests/                      # Unit tests
│   └── test_basic.py
│
├── docs/                       # Documents to evaluate
│   └── sample.md
│
├── requirements.txt            # Dependencies
├── setup.py                    # Package config
├── queries.json                # Test queries
│
├── README.md                   # Quick overview
├── GUIDE.md                    # Complete guide
├── PDF_GUIDE.md                # PDF instructions
├── RESULTS_ANALYSIS.md         # Results explanation
│
├── evaluation_results.json     # Output results
└── show_results.py             # Results viewer
```

---

## 🔄 What Gets Regenerated Automatically

| File/Folder | Regenerated By | When |
|-------------|----------------|------|
| `__pycache__/` | Python | First time you run .py files |
| `*.pyc` | Python | When .py files are imported |
| `.pytest_cache/` | pytest | When you run tests |
| `rag_eval.egg-info/` | pip | When you run `pip install -e .` |
| `evaluation_results.json` | Your CLI | When you run evaluation |

**Safe to delete anytime** - they'll come back when needed.

---

## 📊 File Count Summary

| Category | Count | Notes |
|----------|-------|-------|
| **Essential Code** | 4 files | rag_eval/*.py |
| **Tests** | 1 file | tests/test_basic.py |
| **Config** | 3 files | requirements.txt, setup.py, queries.json |
| **Docs** | 4 files | README, GUIDE, PDF_GUIDE, RESULTS_ANALYSIS |
| **Output** | 2 files | evaluation_results.json, show_results.py |
| **Data** | 1 file | docs/sample.md |
| **Infrastructure** | 2 items | .git/, .venv/ |
| **Total** | ~17 files/folders | Clean and organized |

---

## 🎯 What Each File Does

### When You Want To...

**Run the evaluation:**
```powershell
.venv\Scripts\python -m rag_eval.cli
```
Uses: `cli.py`, `evaluator.py`, `strategies.py`, `docs/`, `queries.json`

**View results:**
```powershell
.venv\Scripts\python show_results.py
```
Uses: `show_results.py`, `evaluation_results.json`

**Run tests:**
```powershell
pytest tests/test_basic.py
```
Uses: `test_basic.py`, `strategies.py`

**Add documents:**
```powershell
cp your_file.pdf docs/
```
Uses: `docs/` folder

**Add queries:**
```
Edit queries.json
```
Uses: `queries.json`

**Install/reinstall:**
```powershell
pip install -r requirements.txt
pip install -e .
```
Uses: `requirements.txt`, `setup.py`

**Learn how to use:**
```
Read README.md → GUIDE.md → PDF_GUIDE.md
```

---

## 🚫 DO NOT DELETE

**Critical files (project breaks without them):**
1. `rag_eval/` folder - Your application
2. `requirements.txt` - Dependencies
3. `setup.py` - Package config

**Important files (lose functionality):**
4. `tests/test_basic.py` - Can't verify code works
5. `queries.json` - Need to recreate queries
6. `docs/sample.md` - Lose example data

**Nice to have:**
7. All `.md` documentation files - Lose instructions
8. `show_results.py` - Harder to view results
9. `evaluation_results.json` - Lose last results (can regenerate)

---

## 💡 Pro Tips

### If Project Feels Cluttered:
```powershell
# Remove all auto-generated files
Remove-Item -Recurse -Force __pycache__, .pytest_cache, *.egg-info
```

### If Dependencies Break:
```powershell
# Reinstall everything
pip install -r requirements.txt
pip install -e .
```

### If Tests Fail:
```powershell
# Clear caches and rerun
pytest --cache-clear tests/test_basic.py -v
```

### Fresh Start on New Machine:
```powershell
git clone <your-repo>
cd Chunk-RAG-Python
python -m venv .venv
.venv\Scripts\Activate.ps1
pip install -r requirements.txt
pip install -e .
pytest tests/
python -m rag_eval.cli
```

---

## ✅ Current Status

After cleanup:
- ❌ Removed `__pycache__/` (4 folders)
- ❌ Removed `.pytest_cache/` (1 folder)
- ❌ Removed `rag_eval.egg-info/` (1 folder)
- ❌ Removed duplicate `tests/sample.md` (1 file)
- ✅ Added `.gitignore` (prevents future clutter)
- ✅ Kept all 17 essential files

**Your project is now clean and organized!**
