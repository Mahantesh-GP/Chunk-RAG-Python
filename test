Before proceeding to tasks, correct the technical plan so it conforms to the clarified spec.md.

Do not apply planning defaults that contradict decisions already captured during /speckit.clarify.

Corrections:

1. Duplicate row IDs:
Do not accept only the first occurrence.
Preserve all otherwise-valid rows with duplicate Excel "#" values and record a warning for each duplicate condition.
The Excel row number is source/display metadata, not the application's unique persistent identity.

2. Import history retention:
Do not retain only the last 100 records.
For V1, retain all import-run metadata/diagnostic records with no automatic purge.
This applies only to import audit/history, not historical rollout snapshots or trend data.

3. Missing required sheet:
If any required initiative sheet is missing, record the blocking validation error, continue validating the remaining sheets/workbooks so all diagnostics are collected, and fail the complete batch at activation.
Partial activation is not allowed.

4. Invalid dates:
Keep the existing decision: invalid optional dates become null with a non-fatal warning and the row is retained.

Update plan.md and research.md where necessary.
Use spec.md and constitution.md as the source of truth.
Do not generate tasks yet.