Before generating tasks, correct the remaining terminology inconsistencies in spec.md.

The source model is:

one or more DR workbooks
→ each DR workbook contains all six required initiative sheets.

Please make these corrections only:

1. In User Story 1 and its acceptance scenarios, remove any wording saying
   "one workbook per initiative". Replace it with one or more DR workbooks,
   each containing all six required initiative sheets.

2. In Edge Cases, remove questions that treat initiatives as separate workbooks.
   Replace them with relevant DR-workbook edge cases such as no workbook
   submitted, missing required initiative sheet, duplicate workbook submission,
   or duplicate DR identity.

3. In the Workbook Submission entity, replace "exactly six sheets" with
   "contains all six required initiative sheets". Do not reject additional
   non-required sheets unless an explicit requirement says to do so.

4. Rename "Unresolved Behaviors (Require Decision Before Planning)" to
   "Resolved Behavior Decisions", since all listed decisions are resolved.

Do not change the previously clarified behavior decisions.
Do not generate tasks yet.